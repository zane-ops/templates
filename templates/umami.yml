x-zane-env:
  APP_SECRET: "{{ generate_password | 64 }}"
  main_domain: "{{ generate_domain }}"
  # Variables PostgreSQL
  POSTGRES_USER: "umami"
  POSTGRES_PASSWORD: "{{ generate_password | 16 }}"
  POSTGRES_DB: "umami"
  DB_SERVICE_ALIAS: "{{ network_alias | 'db' }}"
  DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_SERVICE_ALIAS}:5432/${POSTGRES_DB}
  UMAMI_VERSION: 3.0.3

services:
  umami:
    image: ghcr.io/umami-software/umami:${UMAMI_VERSION:-latest}
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "curl http://localhost:3000/api/heartbeat"]
      interval: 5s
      timeout: 5s
      retries: 5
    depends_on:
      - db
    environment:
      # default umami username=admin
      # default umami password=umami
      DATABASE_URL: ${DATABASE_URL}
      DATABASE_TYPE: postgresql
      APP_SECRET: ${APP_SECRET}
    deploy:
      labels:
        zane.http.routes.0.domain: ${main_domain}
        zane.http.routes.0.port: 3000

  db:
    image: postgres:15-alpine
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5

    volumes:
      - db-data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}

volumes:
  db-data:
